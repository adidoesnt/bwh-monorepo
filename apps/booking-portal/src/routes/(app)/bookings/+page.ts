// Client-side render: coach openness, slot times and the bookings list all
// format instants in the viewer's timezone, and the directory needs the
// browser's own zone. Matches /dashboard.
export const ssr = false;
