---
import { SiteLayout } from "../layouts";
import {
  FaqSection,
  PackageCards,
  SessionFlowChevrons,
  TestimonialCards,
  TrainingCta,
} from "../components";
import { CircleCheckIcon } from "@repo/ui/icons";
import {
  approachNarrative,
  approachPromise,
  approachSectionTitle,
  coachingHeading,
  coachingPillars,
  packagesIntroHtml,
  packagesSectionTitle,
  philosophyParagraphs,
  philosophySectionTitle,
  promiseHeading,
  resonateHeading,
  resonateItems,
  sessionFlowHeading,
  sessionFlowSteps,
  faqSectionTitle,
  testimonialsSectionTitle,
} from "../constants";

const approachCardClass =
  "bg-white rounded-sm p-4 md:p-6 shadow-md flex flex-col gap-4";
---

<SiteLayout>
  <!-- Training Philosophy Section -->
  <section
    id="training-philosophy-section"
    class="flex flex-col gap-4 p-4 md:p-8 bg-base-200"
  >
    <h1
      class="font-body text-dark-brown text-center text-3xl md:text-4xl font-bold tracking-wide"
    >
      {philosophySectionTitle}
    </h1>

    <div class="grid w-full max-w-6xl mx-auto gap-4 md:grid-cols-2 md:items-center">
      <div
        class="order-2 md:order-1 flex flex-col w-full gap-4 font-body justify-center text-lg md:text-xl"
      >
        {
          philosophyParagraphs.map((paragraph) => (
            <p set:html={paragraph} />
          ))
        }
      </div>
      <div
        class="order-1 md:order-2 flex w-full items-center justify-center"
      >
        <img
          src="/training-image-2.png"
          alt="Founder in the gym"
          class="aspect-7/8 w-full max-w-md rounded-sm object-cover"
        />
      </div>
    </div>
  </section>

  <!-- Training Approach Section -->
  <section
    id="training-approach-section"
    class="flex flex-col gap-4 p-4 md:p-8 bg-beige"
  >
    <h1
      class="font-body text-dark-brown text-center text-3xl md:text-4xl font-bold tracking-wide"
    >
      {approachSectionTitle}
    </h1>

    <div
      class="grid w-full max-w-6xl mx-auto gap-4 md:grid-cols-2 md:gap-6 md:items-stretch"
    >
      <div
        class="flex flex-col gap-4 font-body text-dark-brown text-lg md:text-xl"
      >
        <div class={approachCardClass}>
          <h2 class="font-body font-bold">{resonateHeading}</h2>
          <ul class="flex flex-col gap-3">
            {
              resonateItems.map((item) => (
                <li class="flex gap-3 items-center">
                  <CircleCheckIcon className="size-6 text-success shrink-0 mt-0.5" />
                  <span set:html={item} />
                </li>
              ))
            }
          </ul>
        </div>

        <div class={approachCardClass}>
          <h2 class="font-body font-bold">{coachingHeading}</h2>
          <div class="flex flex-col gap-2">
            {
              coachingPillars.map(({ title, description }) => (
                <div class="flex items-center overflow-hidden rounded-sm bg-secondary text-secondary-content">
                  <span class="flex text-center w-1/2 justify-center items-center self-stretch border-r border-secondary-content/20 px-3 py-2 font-bold">
                    {title}
                  </span>
                  <span class="w-1/2 px-3 py-2 text-sm md:text-base">
                    {description}
                  </span>
                </div>
              ))
            }
          </div>
        </div>
      </div>

      <div
        class="flex flex-col gap-4 font-body text-dark-brown text-lg md:text-xl h-full"
      >
        <div class={approachCardClass}>
          <h2 class="font-body font-bold">{sessionFlowHeading}</h2>
          <SessionFlowChevrons steps={sessionFlowSteps} />
        </div>

        <div class={`${approachCardClass} md:text-left text-center`}>
          {approachNarrative.map((paragraph) => <p set:html={paragraph} />)}
        </div>

        <div class={approachCardClass}>
          <div class="flex flex-col">
            <h2 class="font-body font-bold md:text-left text-center">
              {promiseHeading}
            </h2>
            <p class="font-body md:text-left text-center">{approachPromise}</p>
          </div>
        </div>

        <TrainingCta variant="card" />
      </div>
    </div>
  </section>

  <!-- Testimonials Section -->
  <section
    id="training-testimonials-section"
    class="text-dark-brown bg-base-200 flex w-full flex-col items-center gap-4 overflow-x-hidden p-4 md:gap-8 md:p-8"
  >
    <h1
      class="font-body text-center text-3xl md:text-4xl font-bold tracking-wide"
    >
      {testimonialsSectionTitle}
    </h1>
    <div class="w-full min-w-0 max-w-6xl">
      <TestimonialCards client:load />
    </div>
  </section>

  <!-- Training Packages Section -->
  <section
    id="training-packages-section"
    class="text-dark-brown flex w-full flex-col items-center gap-4 overflow-x-hidden p-4 md:gap-8 md:p-8"
  >
    <h1
      class="font-body text-center text-3xl md:text-4xl font-bold tracking-wide"
    >
      {packagesSectionTitle}
    </h1>
    <p
      class="font-body text-center text-lg md:text-xl max-w-4xl"
      set:html={packagesIntroHtml}
    />
    <div class="w-full min-w-0 max-w-6xl">
      <PackageCards client:load />
    </div>
    <div class="w-full min-w-0 max-w-6xl">
      <TrainingCta />
    </div>
  </section>

  <!-- FAQ Section -->
  <section
    id="training-faq-section"
    class="text-dark-brown bg-base-200 flex w-full flex-col items-center gap-4 overflow-x-hidden p-4 md:gap-8 md:p-8"
  >
    <h1
      class="font-body text-center text-3xl md:text-4xl font-bold tracking-wide"
    >
      {faqSectionTitle}
    </h1>
    <div class="w-full min-w-0 max-w-6xl">
      <FaqSection client:load />
    </div>
    <div class="w-full min-w-0 max-w-6xl">
      <TrainingCta />
    </div>
  </section>
</SiteLayout>
